{
  "name": "random-math",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "build": "tsc -p tsconfig.build.json && node _postbuild.mjs",
    "watch": "tsc -p tsconfig.build.json --watch"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vite": "^5.4.8",
    "@types/react": "^18.3.10",
    "@types/react-dom": "^18.3.0"
  }
}
