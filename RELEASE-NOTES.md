# 随机与计算系统 v2.0.0

本版本修正随机与计算的边界行为，新增概率预览、固定记录重置和明确的错误输出。

**升级提示：本次包含不兼容变化，使用新的主版本号 2.0.0。升级前请备份并阅读 [迁移指南](https://github.com/recurse00-stack/random-math/blob/v2.0.0/docs/MIGRATION-2.0.md)。v1.0.0 保留。**

- 单次与批量共用精度格点抽样，统一包含端点与范围校验。
- 固定随机采用槽存档记录，解决默认 0 被误当作已抽取的问题；支持旧值接入与重置。
- 全零权重默认报告错误，均匀回退改为显式选项；提供候选权重和概率检查。
- 修复幂优先级与结合方向，支持一元负号、科学记数法和嵌套表函数。
- 补充有限值、输出类型、缺变量、函数循环与执行预算检查。
- 保留原方法 ID 和数据表契约；使用公开 SDK，无网络、无原生依赖。

验证：157 项逻辑测试通过，构建、类型检查和构建入口回归通过；Studio 1.21.0 参数面板、运行流程及关闭重开后的槽存档恢复已验证。其他宿主版本和导出平台仍需集成验证。

## 下载

- `random-math-v2.0.0.zip`：可导入的插件发行包，包含使用说明和许可证。
- `random-math-v2.0.0-docs.zip`：独立说明文档包。
- `SHA256SUMS.txt`：发行附件校验值。
- GitHub 自动生成的 Source code：源码，需要自行构建。

[使用手册](https://github.com/recurse00-stack/random-math/blob/v2.0.0/docs/USER-GUIDE.md) · [完整更新记录](https://github.com/recurse00-stack/random-math/blob/v2.0.0/CHANGELOG.md)
