# 更新记录

## 2.0.0 — 2026-09-11

本版本包含不兼容的行为修正。旧方法 ID `rand`、`rand-pick`、`calc` 和数据表契约保持；升级时必须检查 [1.0 → 2.0 迁移指南](docs/MIGRATION-2.0.md)。

### 新增

- `reset-fixed`：按记录名清除固定结果，下次重新抽取。
- `preview-pool`：不抽取，输出候选权重与概率的 JSON 报告。
- `successVar` / `errorVar`：明确区分成功与失败，避免将合法 -1 当作错误。
- 命名固定记录、显式接入旧值，以及候选池范围选择。
- 一元正负号、科学计数法、计算与权重上下文中的嵌套表函数。

### 修复与重构

- 固定随机使用槽存档记录，不再把输出默认值 0 当成已抽取结果。
- 单次与批量共用格点采样器；统一端点、精度、范围与数量校验。
- 小数按可选格点等概率抽取，避免连续数四舍五入引入的端点偏差。
- 全零权重默认失败，只有显式启用时才均匀回退；非法/负权重始终报错。
- `^` 高于乘除且右结合：`2*3^2=18`、`2^3^2=512`。
- 检查输出类型与重名；可预见的配置错误保留原结果。
- 检查非有限数、缺变量、函数重名/循环/深度与执行预算。
- 表达式编译缓存有上限；每次执行仍读取当前变量与表数据。
- 完整构建始终同步 `index.js` 和 `index.mjs`。

### 验证范围

58 项基础测试与 99 项边界/存档回归通过；构建、类型检查和 postbuild 回归通过。真实 Studio 1.21.0 已检查参数面板、固定随机、嵌套函数、批量端点、零权重失败、概率报告，以及关闭重开后的槽存档恢复。不同宿主版本、导出平台和复杂流程仍应由使用者做集成验证。

## 1.0.0

初始发行：随机数、加权列表抽取、计算与白名单表达式 DSL。保留原发行包供兼容使用。
